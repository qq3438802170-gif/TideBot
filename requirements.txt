# Web 服务与网络
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
websockets>=12.0
httpx>=0.27.0

# 数据库与 ORM (支持异步 SQLite)
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
alembic>=1.13.1

# 数据校验与配置管理
pydantic>=2.7.4
pydantic-settings>=2.3.4
python-dotenv>=1.0.1
pyyaml>=6.0.1

# 大模型官方 SDK
openai>=1.35.7
# 其他模型通常可以通过 openai 库的 baseURL 兼容，或后续按需引入具体 SDK

# 定时任务调度
apscheduler>=3.10.4

# 安全与加密
pyjwt>=2.8.0
cryptography>=42.0.8